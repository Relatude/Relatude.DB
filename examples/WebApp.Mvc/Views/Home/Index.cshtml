@{
    // Title and Description are set in HomeController.Index; the layout writes them into <title> and <meta name="description">.
}
<h1>WebApp.Mvc</h1>
<p>Hello from Relatude.DB! The database holds @ViewData["NodeCount"] nodes.</p>
<p>
    This page is rendered on the server by <code>Views/Home/Index.cshtml</code> through
    <code>Controllers/HomeController.cs</code>. Node classes go in <code>Models/</code>; the README explains
    how to add pages, models and search.
</p>
