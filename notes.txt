
TODO:
- change tracking. 
		One object for classes ( and interfaces )
		Built in tracking on dynamic generated objects (from interfaces )
- Link property type
- Html parsring and Markdown parsing ( plugins )
   link tracking
- File conversion status page in DBA UI
 - Changs file for dm models
 - Tests for code generation...
 - Simpler common Attributes for "Index"
 - Remote store access
 - Vector index optimazation

   






DBA UI:








Overview:

Notification area: Show reason or error message from database from startup, if any

Add interval selection for all statistics:
Upper right selection: 1m/15m/1h/24h/7d

NODES
	No nodes and relations
	Background no nodes last 15m



















Rules for Meta data

Default QueryContext filter:
- Anonymous user
- No Collection specified
- Hide Hidden
- Only show published items or items with no Meta data
- Culture is set, by default to US, hard coded ( ensure culture method )
- By default, no culture fallbakcs

Results with Default Meta ( Meta.Empty ):
- Included in all culture contexts, both with and without fallbacks filter
- If access empty, then fallback to type default which is either anonymous or admin

New content:
When no Meta Data is specified, leave as null

MetaData can only be changed by Revision Actions

MetaData for NodeData can be null, "empty", "min", or "full"
MetaData for NodeDataRevision is always "full", and is never null
MetaData for NodeDataRevisions is NA, and we use MetaData of first revision 
---

Remember to ensure node data objects put in cache are immutable!!! Also for interface models!

Test interfaces Native Models
Think about revisionId when updating and inserting

Default culture?

Write testst
