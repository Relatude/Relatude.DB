<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- the two benchmarked implementations: MemorySemanticIndex lives in DataStoreLocal (internal,
         reached through InternalsVisibleTo), NativeVectorIndex in its own assembly -->
    <ProjectReference Include="..\..\src\Relatude.DB.DataStoreLocal\Relatude.DB.DataStoreLocal.csproj" />
    <ProjectReference Include="..\..\src\Relatude.DB.VectorIndex\Relatude.DB.VectorIndex.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <InvariantGlobalization>true</InvariantGlobalization>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Version>0.2.0.135</Version>
  </PropertyGroup>

</Project>
