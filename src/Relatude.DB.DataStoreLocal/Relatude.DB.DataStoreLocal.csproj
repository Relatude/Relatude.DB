<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net8.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<RootNamespace>Relatude.DB</RootNamespace>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<Version>0.2.0.147</Version>
	</PropertyGroup>

	<ItemGroup>
		<ProjectReference Include="..\Relatude.DB.DataStore\Relatude.DB.DataStore.csproj" />
		<ProjectReference Include="..\Relatude.DB.KvStore\Relatude.DB.KvStore.csproj" />
	</ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Relatude.DB.Tests" />
    <!-- benchmarks the internal WordIndexTrie as the reference implementation, see examples\TextIndexBenchmarks -->
    <InternalsVisibleTo Include="TextIndexBenchmarks" />
    <!-- benchmarks the internal MemorySemanticIndex against the disk index, see benchmarks\VectorIndexBenchmarks -->
    <InternalsVisibleTo Include="VectorIndexBenchmarks" />
  </ItemGroup>
</Project>
